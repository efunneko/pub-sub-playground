// spring.js - A fixed spring that can be added to the world to have objects bounce off of it

import * as THREE     from 'three'
import {StaticObject} from './static-object.js'
import {Assets}       from '../assets.js'

export class Spring extends StaticObject {
  // TBD - do we really need this?
}